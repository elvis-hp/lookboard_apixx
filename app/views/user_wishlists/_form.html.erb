<%= form_for(@user_wishlist) do |f| %>
  <% if @user_wishlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_wishlist.errors.count, "error") %> prohibited this user_wishlist from being saved:</h2>

      <ul>
      <% @user_wishlist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
