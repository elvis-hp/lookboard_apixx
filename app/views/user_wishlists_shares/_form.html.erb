<%= form_for(@user_wishlists_share) do |f| %>
  <% if @user_wishlists_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_wishlists_share.errors.count, "error") %> prohibited this user_wishlists_share from being saved:</h2>

      <ul>
      <% @user_wishlists_share.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
